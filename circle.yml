deployment:
  production:
    branch: v2
    heroku:
      appname: datarockets
      commands:
        - git push git@heroku.com:datarockets.git v1:master
        - heroku run rake db:migrate --app datarockets
